diff --git a/src/lib/validators/taskValidator.js b/src/lib/validators/taskValidator.js
index 1234567..abcdefg 100644
--- a/src/lib/validators/taskValidator.js
+++ b/src/lib/validators/taskValidator.js
@@ -14,10 +14,6 @@ export const validateTaskData = (data) => {
   } else if (data.title.length > 200) {
     errors.push('title must be 200 characters or less')
   }
-
-  if (data.status !== undefined && !['todo', 'in-progress', 'done'].includes(data.status)) {
-    errors.push('status must be one of: todo, in-progress, done')
-  }

   if (data.priority !== undefined && !['low', 'medium', 'high'].includes(data.priority)) {
     errors.push('priority must be one of: low, medium, high')
